import pkg.*;
public class GameSpinner{
	private int rand;
	private int num;
	private int count;
	public GameSpinner(int i){
		
	}
	public int spin(){//make variable that stores last random, if same as new random increase runcount, return runcount in currentrun
		private int runcount = 0;
		rand = 4;
		num = (int)(Math.random()*rand);
		if(num == runcount){
			
		}
		return num;
	}
	public int currentRun(){
		
	}
}
